let token=localStorage.getItem('adminToken'),foods=[],categories=[],selectedUser=null,poller=null;const $=id=>document.getElementById(id);async function api(url,opt={}){const h={'Content-Type':'application/json'};if(token)h.Authorization='Bearer '+token;const r=await fetch(url,{...opt,headers:h}),d=await r.json().catch(()=>({}));if(!r.ok)throw Error(d.message||'Request failed');return d}function esc(v){return String(v).replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#039;'}[c]))}async function login(e){e.preventDefault();$('error').textContent='';try{const d=await api('/api/auth/login',{method:'POST',body:JSON.stringify({email:$('email').value,password:$('password').value})});if(d.user.role!=='admin')throw Error('This account is not an admin.');token=d.token;localStorage.setItem('adminToken',token);show()}catch(e){$('error').textContent=e.message}}async function show(){try{const me=await api('/api/me');if(me.role!=='admin')throw Error('Admin access required.');$('login').classList.add('hidden');$('dash').classList.remove('hidden');await Promise.all([stats(),loadCategories(),loadFoods(),loadOrders(),loadUsers(),loadChats()]);poller=setInterval(async()=>{try{await loadChats();if(selectedUser)await refreshSelectedChat()}catch{}},3000)}catch(e){localStorage.removeItem('adminToken');token=null;$('error').textContent=e.message}}async function stats(){try{const s=await api('/api/admin/stats');$('foodsN').textContent=s.foods;$('ordersN').textContent=s.orders;$('customersN').textContent=s.customers;$('revenueN').textContent='৳'+Number(s.revenue).toFixed(2)}catch(e){console.error('Stats error:',e)}}async function loadCategories(){try{categories=await api('/api/categories');console.log('Categories loaded:',categories);renderCategories();populateCategorySelect();return categories}catch(e){console.error('Load categories error:',e);$('categoryList').innerHTML='<p>Error loading categories.</p>'}}function renderCategories(){if(!categories||categories.length===0){$('categoryList').innerHTML='<p>No categories available. Add your first category!</p>';return}$('categoryList').innerHTML=categories.map(c=>`<div class="category-card"><span class="icon">${c.icon||'📁'}</span><div class="name">${esc(c.name)}</div><div class="desc">${esc(c.description||'')}</div><div class="food-count">${c.food_count||0} foods</div><div class="actions"><button onclick="editCategory(${c.id})">Edit</button><button class="delete" onclick="delCategory(${c.id})">Delete</button></div></div>`).join('')}function populateCategorySelect(){const select=$('foodCategory');if(!select)return;select.innerHTML='<option value="">Select Category</option>';categories.forEach(c=>{select.innerHTML+=`<option value="${c.id}">${c.icon||'📁'} ${esc(c.name)}</option>`})}function openCategory(c=null){$('categoryFormTitle').textContent=c?'Edit Category':'Add Category';$('categoryId').value=c?.id||'';$('categoryName').value=c?.name||'';$('categoryIcon').value=c?.icon||'';$('categoryDesc').value=c?.description||'';$('categoryModal').classList.remove('hidden')}function closeCategory(){$('categoryModal').classList.add('hidden')}function editCategory(id){let category=null;category=categories.find(c=>c.id===id);if(!category)category=categories.find(c=>String(c.id)===String(id));if(!category){alert('Category not found!');return}openCategory(category)}async function saveCategory(e){e.preventDefault();const id=$('categoryId').value;const body={name:$('categoryName').value.trim(),icon:$('categoryIcon').value.trim()||'📁',description:$('categoryDesc').value.trim()};try{if(id){await api('/api/admin/categories/'+id,{method:'PUT',body:JSON.stringify(body)})}else{await api('/api/admin/categories',{method:'POST',body:JSON.stringify(body)})}closeCategory();await loadCategories();alert(id?'Category updated!':'Category added!')}catch(e){console.error('Save category error:',e);alert('Error saving category: '+e.message)}}async function delCategory(id){const category=categories.find(c=>c.id===id);if(category&&category.food_count>0){if(!confirm(`Category "${category.name}" has ${category.food_count} foods. Delete anyway?`)){return}}else{if(!confirm('Delete this category?'))return}try{await api('/api/admin/categories/'+id,{method:'DELETE'});await loadCategories();await loadFoods();alert('Category deleted!')}catch(e){console.error('Delete category error:',e);alert('Error deleting category: '+e.message)}}async function loadFoods(){try{foods=await api('/api/foods');console.log('Foods loaded:',foods);renderFoods()}catch(e){console.error('Load foods error:',e);$('foodList').innerHTML='<p>Error loading foods.</p>'}}function renderFoods(){if(!foods||foods.length===0){$('foodList').innerHTML='<p>No foods available.</p>';return}$('foodList').innerHTML=foods.map(f=>{const category=categories.find(c=>c.id===f.category_id);return`<article class="food"><img src="${f.image}" alt="${esc(f.name)}"><div class="foodbody"><h3>${esc(f.name)}</h3>${category?`<span style="font-size:12px;color:#0b6b57;">${category.icon} ${esc(category.name)}</span>`:''}<p>${esc(f.description)}</p><b>৳${Number(f.price).toFixed(2)}</b><div class="actions"><button onclick="editFood(${f.id})">Edit</button><button class="delete" onclick="delFood(${f.id})">Delete</button></div></div></article>`}).join('')}function openFood(f=null){$('formTitle').textContent=f?'Edit Food':'Add Food';$('foodId').value=f?.id||'';$('foodName').value=f?.name||'';$('foodDesc').value=f?.description||'';$('foodPrice').value=f?.price||'';$('foodImage').value=f?.image||'';$('foodCategory').value=f?.category_id||'';$('modal').classList.remove('hidden')}function closeFood(){$('modal').classList.add('hidden')}function editFood(id){let food=null;food=foods.find(f=>f.id===id);if(!food)food=foods.find(f=>String(f.id)===String(id));if(!food){alert('Food not found!');return}openFood(food)}async function saveFood(e){e.preventDefault();const id=$('foodId').value;const body={name:$('foodName').value.trim(),description:$('foodDesc').value.trim(),price:Number($('foodPrice').value),image:$('foodImage').value.trim(),category_id:$('foodCategory').value||null};try{if(id){await api('/api/admin/foods/'+id,{method:'PUT',body:JSON.stringify(body)})}else{await api('/api/admin/foods',{method:'POST',body:JSON.stringify(body)})}closeFood();await Promise.all([loadFoods(),loadCategories(),stats()]);alert(id?'Food updated!':'Food added!')}catch(e){console.error('Save food error:',e);alert('Error saving food: '+e.message)}}async function delFood(id){if(!confirm('Delete this food?'))return;try{await api('/api/admin/foods/'+id,{method:'DELETE'});await Promise.all([loadFoods(),stats()]);alert('Food deleted!')}catch(e){console.error('Delete food error:',e);alert('Error deleting food: '+e.message)}}async function loadOrders(){try{const os=await api('/api/admin/orders');$('orderList').innerHTML=os.length?os.map(o=>`<div class="order"><div class="orderTop"><div><h3>Order #${o.id}</h3><b>${esc(o.customer_name)}</b> — ${esc(o.customer_email)}<div>Phone: ${esc(o.phone)}</div><div>Address: ${esc(o.address)}</div></div><b>৳${Number(o.total).toFixed(2)}</b></div><p>Items: ${o.items.map(i=>`${esc(i.food_name)} × ${i.quantity}`).join(', ')}</p><select onchange="status(${o.id},this.value)">${['Pending','Confirmed','Preparing','Out for Delivery','Delivered','Cancelled'].map(s=>`<option ${s===o.status?'selected':''}>${s}</option>`).join('')}</select><small> Current: ${o.status} | ${o.created_at}</small></div>`).join(''):'<p>No orders yet.</p>'}catch(e){console.error('Load orders error:',e)}}async function status(id,s){try{await api('/api/admin/orders/'+id+'/status',{method:'PATCH',body:JSON.stringify({status:s})});await Promise.all([loadOrders(),stats()])}catch(e){alert(e.message)}}async function loadUsers(){try{const us=await api('/api/admin/users');$('userList').innerHTML=us.length?`<div class="tableWrap"><table><thead><tr><th>Name</th><th>Email</th><th>Role</th><th>Orders</th><th>Spent</th><th>Messages</th><th>Joined</th></tr></thead><tbody>${us.map(u=>`<tr><td>${esc(u.name)}</td><td>${esc(u.email)}</td><td>${u.role}</td><td>${u.order_count}</td><td>৳${Number(u.total_spent).toFixed(2)}</td><td>${u.message_count}</td><td>${u.created_at}</td></tr>`).join('')}</tbody></table></div>`:'<p>No users found.</p>'}catch(e){console.error('Load users error:',e)}}async function loadChats(){try{const cs=await api('/api/admin/chats');$('chatList').innerHTML=cs.length?cs.map(c=>`<button class="chatItem ${String(selectedUser)===String(c.user_id)?'active':''}" onclick="loadChat('${c.user_id}')"><strong>${esc(c.name)}</strong><small>${esc(c.email)}</small><span>${esc(c.last_message||'No messages yet')}</span></button>`).join(''):'<p>No conversations yet.</p>'}catch(e){console.error('Load chats error:',e)}}async function loadChat(uid,silent=false){try{selectedUser=uid;const d=await api('/api/admin/chats/'+uid);$('chatTitle').textContent=d.user.name+' — '+d.user.email;$('adminMessages').innerHTML=d.messages.length?d.messages.map(m=>`<div class="chat-message ${m.sender_role==='admin'?'admin-msg':'user-msg'}"><div>${esc(m.message)}</div><small>${m.created_at}</small></div>`).join(''):'<p>No messages yet.</p>';$('adminMessages').scrollTop=$('adminMessages').scrollHeight;$('adminChatForm').classList.remove('hidden');$('adminChatForm').dataset.uid=uid;if(!silent)await loadChats()}catch(e){console.error('Load chat error:',e)}}async function refreshSelectedChat(){if(!selectedUser)return;try{const d=await api('/api/admin/chats/'+selectedUser);const box=$('adminMessages');const nearBottom=box.scrollHeight-box.scrollTop-box.clientHeight<80;const html=d.messages.length?d.messages.map(m=>`<div class="chat-message ${m.sender_role==='admin'?'admin-msg':'user-msg'}"><div>${esc(m.message)}</div><small>${m.created_at}</small></div>`).join(''):'<p>No messages yet.</p>';if(box.innerHTML!==html)box.innerHTML=html;if(nearBottom)box.scrollTop=box.scrollHeight}catch(e){console.error('Refresh chat error:',e)}}async function sendAdmin(e){e.preventDefault();const uid=$('adminChatForm').dataset.uid;const m=$('adminChatInput').value.trim();if(!uid||!m)return;try{await api('/api/admin/chats/'+uid+'/messages',{method:'POST',body:JSON.stringify({message:m})});$('adminChatInput').value='';await loadChat(uid)}catch(e){alert(e.message)}}$('loginForm').addEventListener('submit',login);$('foodForm').addEventListener('submit',saveFood);$('categoryForm').addEventListener('submit',saveCategory);$('adminChatForm').addEventListener('submit',sendAdmin);$('logout').onclick=()=>{localStorage.removeItem('adminToken');location.reload()};if(token){show().catch(e=>{console.error('Initialization error:',e)})}